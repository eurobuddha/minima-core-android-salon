<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <package android:name="org.minimarex.minimacore" />
        <!-- the Maxima transport (DMs over the decentralised comms layer) -->
        <package android:name="com.eurobuddha.maxima.app" />
    </queries>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- signature-level: only family-signed apps can hold it -->
    <uses-permission android:name="com.eurobuddha.maxima.app.permission.USE_MAXIMA" />

    <!-- cleartext ONLY so a self-hosted kubo IPFS API on the LAN
         (http://192.168.x:5001, no TLS) can be reached; public hosting is https. -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.StateNFT"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|density|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Closed-app alerts: the core pushes a targeted NOTIFY broadcast on coin arrival. -->
        <receiver android:name=".SalonNotifyReceiver" android:exported="true">
            <intent-filter>
                <action android:name="org.minimarex.minimacore.NOTIFY" />
            </intent-filter>
        </receiver>
        <!-- The Maxima transport talks back here (replies, inbound DMs, events).
             Guarded by Maxima's signature permission: only family-signed apps
             can feed it, so DELIVER cannot be forged by a third party. -->
        <receiver android:name=".MaximaLinkReceiver" android:exported="true"
            android:permission="com.eurobuddha.maxima.app.permission.USE_MAXIMA">
            <intent-filter>
                <action android:name="com.eurobuddha.maxima.app.RESPONSE" />
                <action android:name="com.eurobuddha.maxima.app.DELIVER" />
                <action android:name="com.eurobuddha.maxima.app.EVENT" />
            </intent-filter>
        </receiver>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
